import { Inter, Sen } from 'next/font/google';

export const inter = Inter({ weight: ['400', '500', '700', '900'], subsets: ['latin'] });

export const sen = Sen({ weight: ['700'], subsets: ['latin'] });
